const sections = [
  {
    title: "Content Pipeline",
    description:
      "See what is being planned, filmed, edited, ready to post, and published.",
    status: "Build next",
  },
  {
    title: "Platforms",
    description:
      "Manage Facebook, TikTok, Instagram, YouTube, and other publishing channels.",
    status: "Planned",
  },
  {
    title: "Content Library",
    description:
      "Keep finished videos, captions, hooks, series, and reusable content organized.",
    status: "Planned",
  },
  {
    title: "Ideas",
    description:
      "Capture content ideas immediately so good concepts do not disappear.",
    status: "Planned",
  },
  {
    title: "Analytics",
    description:
      "Track audience growth, reach, engagement, and which content actually works.",
    status: "Planned",
  },
  {
    title: "Monetization",
    description:
      "Track creator revenue, affiliate opportunities, sponsorships, and income-producing content.",
    status: "Planned",
  },
];

export default function SocialMediaPage() {
  return (
    <main className="min-h-screen bg-[#F3EFE7] px-6 py-8 text-[#2C2C2C] lg:px-8">
      <div className="mx-auto max-w-7xl">
        <header className="mb-8">
          <p className="text-sm font-semibold uppercase tracking-[0.28em] text-[#B08D57]">
            Creator Workspace
          </p>

          <h1 className="mt-3 text-4xl font-semibold text-[#1E3A34]">
            Social Media
          </h1>

          <p className="mt-2 max-w-2xl text-[#6F776B]">
            Plan, create, publish, repurpose, and measure content across your
            social platforms.
          </p>
        </header>

        <section className="mb-8 rounded-2xl border border-[#D7D0C5] bg-[#F8F5EF] p-6">
          <p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#B08D57]">
            Current focus
          </p>

          <h2 className="mt-2 text-2xl font-semibold text-[#1E3A34]">
            Build a repeatable content engine
          </h2>

          <p className="mt-2 text-sm leading-6 text-[#6F776B]">
            Create once, distribute intelligently, learn what performs, and
            turn successful content into repeatable systems.
          </p>
        </section>

        <section className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">
          {sections.map((section) => (
            <article
              key={section.title}
              className="rounded-2xl border border-[#D7D0C5] bg-[#F8F5EF] p-6"
            >
              <div className="flex items-start justify-between gap-4">
                <h2 className="text-xl font-semibold text-[#1E3A34]">
                  {section.title}
                </h2>

                <span className="whitespace-nowrap rounded-full border border-[#D7D0C5] bg-white px-3 py-1 text-xs font-semibold text-[#7C5F33]">
                  {section.status}
                </span>
              </div>

              <p className="mt-4 text-sm leading-6 text-[#6F776B]">
                {section.description}
              </p>
            </article>
          ))}
        </section>

        <section className="mt-8 rounded-2xl border border-[#B08D57]/40 bg-[#F8F5EF] p-6">
          <p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#B08D57]">
            Jarvis principle
          </p>

          <p className="mt-3 text-lg font-medium text-[#1E3A34]">
            Every piece of content should have a purpose, a destination, and
            the potential to be reused.
          </p>
        </section>
      </div>
    </main>
  );
}
